# Vishwaneed Admin Panel

Run from the repository root with `npm run dev:admin`, then open `http://localhost:5175`.

The panel consumes only authenticated admin APIs. Financial amounts, state transitions, eligibility, refunds and commission calculations remain backend-authoritative.

Provider-backed transfer reconciliation, notification-template editing, product binary uploads and general policy configuration require additional backend APIs and credentials.
